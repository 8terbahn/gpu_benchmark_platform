apiVersion: v2
name: benchmark-platform
description: |
  Helm chart for the GPU Benchmark Platform Kubernetes control plane.
  Deploys the BenchmarkJob Operator (Go) alongside the Python FastAPI backend.
type: application
version: 0.1.0
appVersion: "1.0.0"
keywords:
  - gpu
  - benchmark
  - kubernetes
  - operator
  - platform-engineering
maintainers:
  - name: 8terbahn
